"""Edge-case corpus pending manual linguistic review.

These items are intentionally separated from the main gold test set so they
can be discussed and promoted only after consensus on expected behavior.
"""

# Word -> expected transcription proposals for debated forms.
edge_cases = {
    # Nahuatl-origin forms with variable <x> behavior.
    'Xochipilli': "so.tSi.'pi.jji",
}
